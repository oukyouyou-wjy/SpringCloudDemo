spring:
  application:
    name: provider-demo

server:
  port: 8115

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8089/eureka